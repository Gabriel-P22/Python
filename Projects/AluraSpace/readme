Como criar um projeto em DJANGO:

django-admin startproject project_name .

Por convemsão, usamos o nome do projeto como config/setup

django-admin startproject setup .

Podemos usar o pip freeze > requirements.txt para salvar as libs intaladas no arquivo de dependencias
